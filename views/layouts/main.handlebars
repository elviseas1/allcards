<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>All.</title>
        <link rel="shortcut icon" href="/img/logo.ico" type="image/x-icon">
        <link rel="stylesheet" href="/css/styles.css">
        <!-- BOOTSTAP ICONS -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    </head>
    
    <body>
        <nav id="navbar">
            <a href="/" id="logo">
                <img src="/img/logo.png" alt="Card">
            </a>
            <ul>
                
                <!-- URL após autenticação -->
                {{#if session.userid.id}}
                    <li><a href="/">Início</a></li>
                    <li></li>
                    <li><a href="/cardUser/{{session.userid.id}}">Cartão</a></li>
                    <li></li>
                    {{#if session.userid.acesso}}
                    
                        
                    {{else}}
                        <li><a href="/clients">Cliente</a></li>
                        <li></li>
                        <li><a href="/cards">Cartões</a></li>
                        <li></li>
                        <li><a href="/register">Registrar</a></li>
                        <li></li>
                        
                    {{/if}}

                    <li><a href="/logout">Sair</a></li>
                {{else}}
                    <li><a href="/login">Entrar</a></li>
                {{/if}}
                
            </ul>
        </nav>
        <div class="container">
            <!-- Mensagens -->
            {{#if messages.message}}
                <div class="message">
                    {{messages.message}}
                </div>
            {{/if}}
            {{{ body }}}
        </div>

        <footer>
            <p><span>CardsAll</span> &copy; 2023</p>
        </footer>
    </body>
</html>